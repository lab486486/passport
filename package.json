{
  "name": "passport",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pages:dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-08-15",
    "deploy": "npm run build && wrangler pages deploy dist --project-name passport"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.8.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.8.0"
  }
}
